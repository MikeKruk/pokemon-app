export const BASE_URL_API = 'https://pokeapi.co/api/v2/' as const;
export const LOCAL_STORAGE_KEY = 'pokemon-favorites' as const;
export const MAX_LIMIT = 24 as const;
export const ROUTES = {
	HOME: '/',
	FAVORITES: '/favorites',
} as const;
